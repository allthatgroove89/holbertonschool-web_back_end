#!/usr/bin/env python3
"""
This module uses a function to calculate the floor
of a float
"""

import math


def floor(n: float) -> int:
    """
    Returns the floor of a float using the math.floor() function.

    Uses basic annotations for Args.

    """
    return math.floor(n)
